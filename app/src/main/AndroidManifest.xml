<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.donation">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="app.donation.main.DonationApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="app.donation.activity.Welcome"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="app.donation.activity.Report"
            android:label="@string/app_name" />
        <activity
            android:name="app.donation.activity.Donate"
            android:label="@string/app_name" />
        <activity
            android:name="app.donation.activity.Signup"
            android:label="@string/app_name" />
        <activity
            android:name="app.donation.activity.Login"
            android:label="@string/app_name" />
    </application>

</manifest>